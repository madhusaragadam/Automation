package appfactory.Constants;

import appfactory.auth.Authenticator;
/**
 * This class holds all the constant strings related to authentication, which are initialized at run time.
 *
 */
public class AuthConstants {

	public static String auth_url;
	public static Authenticator auth;
}
